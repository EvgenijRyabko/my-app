import React from 'react';
import classes from './Settings.module.css';

function Settings() {
  return (
    <section className={classes['settings-list']}>
      <div>Settings</div>
    </section>
  );
}

export default Settings;
